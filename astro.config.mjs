import { defineConfig } from "astro/config";

// Webflow Cloud sets base path and asset prefix at build time from the
// environment mount path, so keep this config intentionally minimal.
export default defineConfig({});
